// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebaseConfig: {
    apiKey: 'AIzaSyAk66D11aaFSgyFlynRxsjS4QxJ5tggU9o',
    authDomain: 'sunbound-h.firebaseapp.com',
    projectId: 'sunbound-h',
    storageBucket: 'sunbound-h.appspot.com',
    messagingSenderId: '589918480283',
    appId: '1:589918480283:web:33dc43cb414ccf22fa233e',
    measurementId: 'G-GRHHHQ7PPR'
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
